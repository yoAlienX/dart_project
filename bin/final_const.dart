void main() {
  final String name;
  name = "two";
  // final can be declared and initialised in the same line or different lines but can be initialized only once

  const int age = 25; // const should be initialised when declared so that ebin can oomfal.

  print(name);
  print(age);
}
